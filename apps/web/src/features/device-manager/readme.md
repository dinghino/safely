# Device manager

This module contains context and api to manage the core interaction between
the device and the backend, allowing to (un)register the device from the system
and handling heartbeat. calls.

It is also in charge of updating the location manager settings when device settings
change.
