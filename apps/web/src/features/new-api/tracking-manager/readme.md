# Tracking manager

This module exposes functionalities to manage the active device sessions, active
or past, dispatch updates if and when necessary and so on.

To function as intended it needs to be able to interact (be a child) of a `LocationManager`
and a `DeviceManager` contexts.
