# Headless functions and helpers

This module contains functionalities used in `/index.ts` to respond to the
background geolocation plugin events when running in headless mode.

It exists just to split code and responsibilities between modules.
