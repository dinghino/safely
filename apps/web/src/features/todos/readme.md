# Todos feature

Simple todo management. code comes from the starter example but it is always a nice
to have.

## Requirements

Todos are user owned, so everything needs to be rendered inside
an authorization context where the current user can be read.
