# nuqs monorepo

This package handles fixing the context issue with nuqs when using it in a monorepo,
where the created context (app) doesn't match the consumer (packages) and it throws.

setup. see <https://github.com/47ng/nuqs/issues/798>.

It is an ugly solution but it seems to work.
